# From-scratch tracing tutorial — ZERO third-party dependencies.
# Everything uses the Python standard library (contextvars, dataclasses, time).
# Python 3.10+ recommended (uses `X | Y` type hints and dataclass features).
