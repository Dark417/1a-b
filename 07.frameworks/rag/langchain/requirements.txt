# LangChain RAG tutorial — pinned deps (all pure-python / CPU).
# Every example degrades to the offline harness if these are missing, so the
# install is optional; pin for reproducibility when you DO want live APIs.

langchain-core>=0.3,<2
langchain-community>=0.3
langchain-text-splitters>=0.3

# vector stores / retrievers exercised live
faiss-cpu>=1.7
chromadb>=0.5
rank-bm25>=0.2
scikit-learn>=1.3
numpy>=1.24

# OPTIONAL — real local models (not required; harness mock used otherwise):
#   langchain-huggingface   sentence-transformers   # local embeddings
#   langchain-ollama                                  # local chat model (ollama pull llama3.2)
